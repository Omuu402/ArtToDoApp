<?xml version="1.0" encoding="utf-8"?>
<!--
    AndroidManifest.xml — app entry point declaration.

    Key settings:
    - android:theme       : applies Theme.ArtistToDo to the whole app
    - android:exported    : true on MainActivity so the launcher can open it
    - INTERNET permission : not needed (all data is local Room database)
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ArtistToDo">

        <!--
            MainActivity is the only Activity in the app.
            All navigation happens inside it via fragments.
        -->
        <activity
            android:name="com.patelomkumar.arttodoapp.MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">

            <!-- Makes this the launcher Activity (app icon tap opens this) -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

    </application>

</manifest>
